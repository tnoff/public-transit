# Valid date regex to pass to bart urls
DATE_REGEX = "^[0-9]{2}/[0-9]{2}/[0-9]{4}$"
# Valid direction regex to pass to bart urls
DIRECTION_REGEX = "^[nsNS]$"
# Valid station lower case regex to pass to bart urls
STATION_REGEX = "^[a-zA-Z0-9]+$"
