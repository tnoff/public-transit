name: Tag build

on:
  push:
    branches:
    - main

jobs:
  tag_build:
    uses: tnoff/github-workflows/.github/workflows/tag.yml@c4ed1a1868ec99e638528422174dacf1bd9b0afb
    with:
      version_file: ./VERSION
