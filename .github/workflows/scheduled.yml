name: Scheduled

# Replaces the GitLab weekly pipeline schedule, deactivated in terraform!258.
# Same cron terraform derived for it, so the cadence is unchanged.

on:
  schedule:
    - cron: '58 6 * * 1'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  renovate:
    name: Renovate
    uses: tnoff/github-workflows/.github/workflows/renovate.yml@362a5f0d06fec04588c60863ccf0bf2ce709917f
    secrets:
      app_client_id: ${{ secrets.CI_APP_CLIENT_ID }}
      app_private_key: ${{ secrets.CI_APP_PRIVATE_KEY }}

  branch-cleanup:
    name: Branch cleanup
    permissions:
      contents: write
      pull-requests: read
    uses: tnoff/github-workflows/.github/workflows/branch-cleanup.yml@362a5f0d06fec04588c60863ccf0bf2ce709917f
    with:
      age_days: 30
      dry_run: false
